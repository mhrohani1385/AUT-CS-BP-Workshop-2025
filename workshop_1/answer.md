#### نوشته شده در نرم افزار obsidian با markdown

---
## سوال اول - تولد 

```c
#include <stdio.h>   
#include <stdbool.h>

// Code by mhr85

int main() {

    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);

    // Check a with b and c and then check b with c
    if (a == b || a == c || b == c) {
        printf("hamino bekhar");
    } else {
        printf("ino nakhar");
    }

    // to make output more readable
    printf("\n");
    
} 
```


از || به عنوان یا بین شرط ها استفاده شده است که در حقیقت یک عملگر با دو ورودی عددی است که بر روی دو عدد در دستگاه دودویی عملیات یا را اجرا می کند.خروجی آن 0 یا 1 است و فقط در صورتی خروجی آن 0 است که دو عدد دو سمت آن هر دو 0 باشند. ما از این عملگر برای عملیات یا بین خروجی عملگر های مقایسه ای استفاده می کنیم که خروجی آنها در حقیقت یک اینتجر مساوی 0 یا 1 است.

---
## سوال دوم - چاق ای گس؟

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int mass, height;
    scanf("%d %d", &height, &mass);
    
    // convert height from cm to m; i use the deitel and deitel book pdf and search convert to learn how to convert height in cm to m with fractional part.
    float height_m = ((float)height) / 100;

    // calculating bmi, its a mess that i was using ^ operator for make height on power 2 !
    float bmi = mass / (height_m * height_m);

    if (bmi > 30){
        printf("1.0");
    } else {
        printf("0.5");
    }

    // to make output more readable
    printf("\n");
    
}
```

برای تبدیل قد به متر ابتدا آنرا در حالت اعشاری در آورده ایم سپس با توجه به اینکه واحد ها با فرمول همخوانی پیدا کرده اند آنها را در فرمول بی ام ای قرار داده ایم. سپس با بررسی بی ام آی فرد مورد نظر مشخص کرده ایم که یک پیتزا به شخص برسد یا نصف آن. من قبلا برنامه نویسی کرده ام و البته کتاب دایتل انددایتل را هم مطالعه می کنم که در بخش تقسیم به تبدیل کردن اعداد به اعداد اعشاری برای باقی ماندن جزء اعشاری تقسیم اشاره شده. همچنین با تایپ کست در جاوا آشنا بودم در این مورد قبلا با سی پلاس پلاس هم کار کرده بودم و نیازی به پرسش از چت بات یا جستجوی وب نبود.

---
## سوال سوم - مجموع ارقام

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int num; // for example 4581
    scanf("%d", &num);

    // the number has 4 digits so we need to split the digits
    // last three digits (from left):
    int last_3 = num % 1000; // for example 581
    // and as same last two :
    int last_2 = num % 100; // for example 81
    // and as same last one :
    int digit_4 = num % 10; // last one from the left (rightmost) // for example 1

    // we can produce each digit of the four digits with this:
    int digit_1 = (num - last_3) / 1000;
    int digit_2 = (last_3 - last_2) / 100;
    int digit_3 = (last_2 - digit_4) / 10;
    // we have digit 4 from past

    // also we can use this(an inspiration from my friend to get numbers from right with division):
    // dig4 = num % 10;
    // dig3 = (num / 10) % 10;
    // dig2 = (num / 100) % 10;
    // dig1 = (num / 1000);

    int sum = digit_1 + digit_2 + digit_3 + digit_4;
    printf("%d\n", sum);
    
}
```

در این تمرین من از یک الگوریتم طولانی برای جدا کردن ارقام از یکدیگر استفاده کردم که البته یکی از دوستان هم خوابگاهی روش بهتری هم بعد از آن به من پیشنهاد داد که من در این مورد آنرا کامنت قرار دادم ولی در سوالات بعدی که نیاز به جدا سازی ارقام بود (سوال ۸ ام - خود مقلوب) از همین روش دوستم استفاده کردم.

- در این روش که خودم نوشته ام ابتدا سه رقم اول از سمت راست جدا شده و از خود عدد کم شده اند تا ضریبی از 1000 برای رقم اول از سمت چپ بدست آید. سپس با تقسیم این عدد بر هزار رقم اول از سمت چپ بدست آمده.
- برای رقم دوم از سمت چپ دو رقم سمت راست را از سه رقم سمت راست کم کرده ام تا مضربی از 100 بدست آید و با تقسیم این عدد بر صد، رقم مورد نظر مشخص شود.
- برای رقم سوم از سمت چپ رقم سمت راست را از دو رقم سمت راست  کم کردم تا عددی مضرب 10 بدست آید و ...
- برای رقم سمت راست هم (رقم ۴ ام از سمت چپ) باقیمانده خود عدد بر 10 را محاسبه کردم.

سپس با جمع زدن ارقام و چاپ مجموع سوال حل شده است. (راه طولانی و غیر بهینه)

---
## سوال چهارم - بالاترین نمره چند بود؟

```c
#include <stdio.h>   

// Code by mhr85

int main() {


    int number1; // the top-of-mind value in the mind of saman
    int number2 = -1; // -1 for init
    // This loop reads integers until EOF or invalid input

    while (scanf("%d", &number1) == 1) {  // scanf returns number of successfully read items
        if (number1 > number2) {
            number2 = number1;
        }
    }
    
    // CLEANUP/TERMINATION BLOCK
    printf("%d\n", number2);

}
```

در این سوال یک عدد، به عنوان عددی که از کاربر گرفته می شود (بالای ذهن سامان قرار می گیرد) و یک عدد به عنوان عددی که در حافظه ذخیره می شود (بزرگ ترین نمره پس ذهن سامان - که بزرگ ترین عدد بین عددهایی است که تا به حال در بین فرایند وایل از کاربر گرفته شده اند) در نظر گرفته شده اند.

تابع اسکن اف در صورتی که عدد دریافت نکند دوباره از کاربر عدد خواهد خواست (برنامه روی اینتراپت باقی می ماند و هنوز خروجی تابع اسکن اف به برنامه بر نمی گردد) و درصورتی که مقدار نا معتبر غیر عددی یا سیگنال EOF دریافت کند عددی کوچک تر از 1 باز می گرداند(در EOF منفی یک و در مقدار نا معتبر 0 و در مقدار معتبر تعداد متغییر های با موفقیت دریافت شده)

پس برنامه به گرفتن ورودی و بررسی بزرگ تر بودن آن از بزرگ ترین ورودی بین ورودی های قبلی ادامه می دهد تا زمانی که کاربر سیگنال پایان سری داده را بفرستد. پس از آن عدد ۲ که همیشه بزرگ ترین عدد تا به حال بوده چاپ می شود و برنامه به مرحله پایانی خود می رسد.

---
## سوال پنجم - مجموع اعداد

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int num;
    scanf("%d", &num);

    printf("%d", (num * (num + 1)) / 2);
    
}
```

در این سوال از فرمول ریاضی مربوطه استفاده شده است. انجام آن با حلقه به این صورت است که ارسال نکردم:

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int num;
    scanf("%d", &num);
	
	int sum = 0; // variable initialization
    int counter = 1; // represents the number we are processing now;
    
    while(counter <= num) {
	    sum = sum + counter++; // or sum += counter++;
    }
    
    printf("%d\n", sum);

}
```

از عملگر ++ بعد counter استفاده می کنیم تا پس از برگرداندن مقدار به کانتر یکی اضافه شود. (پس-فزاینده)
امیدوارم فکر نکنید تقلب کردم اینها رو جلو به جلو خوندم تو دایتل اند دایتل تو جاوا هم کار کردم. 😇

---
## سوال ششم - مجموع با قدر نسبت

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int n, d;
    scanf("%d %d", &n, &d);

    // START: n correction
    int diff = n - 1;
    int count_we_move_forward = diff / d;
    int count = count_we_move_forward + 1; // count of items in the sequence

    n = 1 + count_we_move_forward * d;
    // END: n correction; now, n is the last item in the sequence

    // printf("c:%d, n:%d", count, n); // for debug

    float avrg = (float)(n + 1) / 2;

    int sum = avrg * count;

    printf("%d\n", sum);
    
}
```

- توسط متغیر diff اختلاف بین جمله اول و آخر به دست آمده - توجه کنید که n داده شده توسط کاربر ممکن است ایراد داشته باشد و خودش جمله آخر نباشد و ما باید آنرا اصلاح کنیم. (کامنت های کد را بخوانید) پس این متغییر دقیق نیست و فقط اختلاف بین عدد 1 و عدد وارد شده توسط کاربر که ممکن است ایراد داشته باشد است.
- سپس تعداد جملاتی که ما از بعد جمله اول (عدد یک) به جلو حرکت کرده ایم بدست آمده (در اینجا با تقسیم اینتجر با اینتجر بخش اعشاری را حذف می کنیم و این مقدار درست خواهد بود حتی اگر کاربر دقیقا عدد مختص جمله آخر را وارد نکرده باشد)
- سپس تعداد جملات را با توجه به اینکه جمله اول را نشمرده ایم محاسبه می کنیم.
- سپس مقدار n را اصلاح کرده و برابر با دقیقا آخرین جمله دنباله قرار می دهیم. ( جمله اول + تعداد جملاتی که جلو رفته ایم ضرب در قدر نسبت)
- میانگین را از جمع کردن جمله ااول و آخر (1 و n) و تقسیم آنها بر ۲ بدست می آوریم
- میانگین را ضرب در تعداد جملات می کنیم تا مجموع جملات بدست آید.
توجه کنید که در این سوال هم از تایپ کست فلوت استفاده کرده ایم تا اگر میانگین اعشاری بود اعشار آن حفظ شود. می توانستیم مجموع جمله اول و آخر را بدون تقسیم بر دو و تایپ کست محاسبه کرده در تعداد ضرب کنیم سپس بر ۲ تقسیم کنیم. چون مطمئن بودیم که این عدد زوج است نیاز به تایپ کست نداشتیم
---
## سوال هفتم - مربع کامل خوب

```c
#include <stdio.h>   
#include <math.h>

// // Code by mhr85

int main() {

    int num;
    scanf("%d", &num);
    
    // i search the google for pow function in c and i found https://www.geeksforgeeks.org/c/power-function-c-cpp/
    // i have an idea to use pow(num, 0.5) instead of using sqrt func

    int correct_guess = (int)pow(num, 0.5);

    if (correct_guess * correct_guess == num) {
        printf("YES\n");
    } else {
        printf("NO\n");    
    }

}
```

در این سوال از تابع pow برای محاسبه جذر عدد استفاده شده است. می توانستیم خودمان حدس بزنیم.
در این سوال با توجه به چیزی که از قبل در ذهنم در مورد تابع pow بود (که آن را جامع تر از sqrt می دانیم) در گوگل روش استفاده آنرا جستجو کردم. خروجی آنرا به صورت اینتجر ذخیره کردم تا اگر کاربر عددی وارد کرده بود که غیر مربع کامل باشد و جذر آن اعشاری شود، نیازی به محاسبات اعشاری ایجاد نشود و یا به علت دقت بالای کامپیوتر و برابر شدن حاصل ضرب اعداد اعشاری مورد نظر با عدد داده شده توسط کاربر دچار اشتباه نشویم.

البته من در مورد اینکه راهی جز اینکه از توابع آماده استفاده کنیم وجود دارد یا نه از مایکروسافت کوپیلوت سوال کردم و به این کد هم رسیدم که پس از تست متوجه بهینگی بشدت پایین آن نسبت به کد بالا شدم :

```c
#include <stdio.h>   
#include <math.h>

// // Code by mhr85


// Function to compute square root using Newton-Raphson method
int sqrt_newton_guess_int(int number) {

    int guess = round(number / 2.0);

    while (1) {
        // printf("guess:%d", guess); // for debug
        int next_guess = round(0.5 * (guess + number / guess));
        if (next_guess == guess)
            break;
        guess = next_guess;
    }

    return guess;

}

int main() {

    for (int i = 0; i < 100000000; i++)
    {
        int num = 178;
        // scanf("%d", &num);
        
        // i search the google for pow function in c and i found https://www.geeksforgeeks.org/c/power-function-c-cpp/
        // i have an idea to use pow(num, 0.5) instead of using sqrt func

        // also i ask this from copilot : what is the nowadays computers algorithm to calculate square root of a floating point number?
        // and the answer is this https://copilot.microsoft.com/shares/AqCkPkp7jhn3stzeGpuVN
        // it seems the most efficient way to guess a correct number of perfect squarw for an integer is implementing the Newton-Raphson method with integers (not floats)
        // if number is perfect_square the function will provide us the correct square root. let check it:

        int correct_guess = sqrt_newton_guess_int(num);

        if (correct_guess * correct_guess == num) {
            // printf("YES\n");
        } else {
            // printf("NO\n");    
        }
    }

}
```

کد تابع sqrt_newton_guess را از چت بات دریافت کردم و با اصلاح آن و تبدیل آن به خروجی دهنده int با تابع round برای بهینگی بیشتر، آنرا قرار دادم. چون ما فقط نیاز داریم یک حدس عددی صحیح را در خودش ضرب کنیم و ببینیم که آیا با رسیدن به عدد کاربر مشخص شده که عدد مربع کامل است یا نه.

سوال : 
> what is the nowadays computers algorithm to calculate square root of a floating point number?
سوال و جواب من با چت بات : https://copilot.microsoft.com/shares/AqCkPkp7jhn3stzeGpuVN

من در مورد اینکه حدس نیوتن چگونه کار می کند اطلاعی ندارم فقط میدانم که هرگاه حدس نیوتن برابر با جذر عدد شود با قرار دادن آن ‍‍‍‍```int next_guess = round(0.5 * (guess + number / guess));``` به خود آن می رسیم و مقدار آن ثابت و برابر با جذر عدد کاربر باقی می ماند.

این الگوریتم کاملا جواب گو بود و تست ها را با موفقیت گذراند ولی با تست gprof مشخص شد که اصلا بهینه نیست.

کامنت های انگلیسی توسط خودم روی کد نوشته شده است که به موارد فوق اشاره دارد.

---

## سوال هشتم - خود مقلوب

```c
#include <stdio.h>   

// Code by mhr85

int main() {

    int num; // for example 4581
    scanf("%d", &num);

    int dig1, dig2, dig3, dig4;
    // i get this inspiration from my friend. before that i wrote it with the way i have do it in the homework_3 (majmooe_arghaam)
    // he say to get numbers from righ instead of left with division
    // you can say my own way in the homework_3 (majmooe_argham)
    
    
    // ANOTHER WAY TO SPLIT DIGITS::
    
    // also we can do it with getchar() and then converting four chars to the integers and compare them(it works, i tested it):
    // dig1 = (int) getchar() - 48; // in ascii table numbers start from 48
    // dig2 = (int) getchar() - 48;
    // dig3 = (int) getchar() - 48;
    // dig4 = (int) getchar() - 48;

    dig4 = num % 10;
    dig3 = (num / 10) % 10;
    dig2 = (num / 100) % 10;
    dig1 = (num / 1000);

    if (dig1 == dig4 && dig2 == dig3) { // tagharon
        printf("YES\n");
    } else {
        printf("NO\n");
    }

}
```
همانطور که در [[## سوال سوم - مجموع ارقام]] اشاره شده من در این سوال از روشی که دوستم گفت استفاده کردم. من روش قبلی خودم رو برای دوستم گفتم و اون گفت بجای این کار اعداد رو یکی یکی از سمت راست جدا کنم. من هم با توجه به حرف دوستم این کار رو کردم. البته اون گفت که هر بار ورودی رو تقسیم بر 10 می کرده و رقم سمت راست رو با باقیمانده بر ده جدا می کرده و مجددا همین کار. ولی من بدون دستکاری عدد ورودی هر رقم رو در یک خط جدا کردم. 
یک روش دیگه هم که به ذهن خودم رسید استفاده از getchar بود که اون رو هم پیاده سازی کردم و بعد به صورت کامنت شده در کد اصلی قرار دادم که ببینید. (کامنت های انگلیسی خوانده شود). فقط برای تبدیل char به int (که میشد انجامش نداد و همون char ها رو مقایسه کرد با هم) تو استک اور فلو سرچ زدم.

در این سوال هم از عملگر بین شرطی && استفاده شده که در دو طرف آن یک عدد (یا خروجی گذاره های شرطی که در حقیقت آنها هم عدد 0 یا 1 اند) قرار می گیرد. این عملگر فقط در صورتی 1 بر می گرداند که هیچ کدام از اعداد دو طرف 0 نباشند. خروجی آن فقط 0 یا 1 است. در حقیقت با استفاده از این عملگر بین دو گذاره شرطی فقط در صورتی حاصل true یا 1 است که هر دو گذاره درست یا 1 باشند.

---

**با تشکر**
**پایان**
محمد حسین روحانی
